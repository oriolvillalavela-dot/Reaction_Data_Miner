[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "surf_extractor"
version = "0.1.0"
description = "Multi-agent LLM pipeline for standardizing chemical reaction data."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-settings>=2.3.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "rdkit-pypi>=2023.03.3",
    "pdfplumber>=0.10.0",
    "PyMuPDF>=1.24.0",
    "python-dotenv",
    "httpx",
    "python-multipart",
    "portkey-ai>=1.9.0",
    "cirpy>=1.0.2",
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = ["pytest", "black", "isort", "jupyter"]

[tool.black]
line-length = 88
